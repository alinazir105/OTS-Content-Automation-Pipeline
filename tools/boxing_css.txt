
/* ===== MULTIPLICATION BOXING ARENA ===== */
.boxing-teaser-section { background: linear-gradient(180deg, #1a2330 0%, #2c3e50 45%, #1e2d40 100%); color: #fff; }
.boxing-teaser-section .section-label { background: var(--amber); }
.boxing-teaser-section h2 { color: #fff; }
.boxing-teaser-section > div > p { color: rgba(255,255,255,0.88); }
.boxing-teaser-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 20px; margin-top: 24px; }
.boxing-teaser-card { background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); border-radius: 18px; padding: 24px; }
.boxing-teaser-card h3 { font-family: 'Poppins', sans-serif; color: #fff; margin-bottom: 10px; font-size: 1.15rem; }
.boxing-teaser-card p { font-size: 0.95rem; line-height: 1.55; color: rgba(255,255,255,0.85); }
.boxing-launch-btn { margin-top: 20px; background: var(--amber); color: #fff; border: none; padding: 16px 36px; border-radius: 999px; font-family: 'Poppins', sans-serif; font-weight: 800; font-size: 1.05rem; cursor: pointer; box-shadow: 0 8px 28px rgba(240,147,43,0.45); transition: transform 0.2s; }
.boxing-launch-btn:hover { transform: translateY(-2px) scale(1.02); }

body.boxing-no-scroll { overflow: hidden; }
.boxing-shell { position: fixed; inset: 0; z-index: 10050; background: #0a0e14; display: none; flex-direction: column; opacity: 0; transition: opacity 0.35s; }
.boxing-shell.is-open { display: flex; opacity: 1; }
.boxing-shell[hidden] { display: none !important; }
.boxing-close { position: absolute; top: 14px; right: 14px; z-index: 30; background: rgba(255,255,255,0.12); color: #fff; border: 1px solid rgba(255,255,255,0.2); padding: 10px 18px; border-radius: 999px; font-family: 'Poppins', sans-serif; font-weight: 700; cursor: pointer; }
.boxing-intro, .boxing-end { margin: auto; max-width: 480px; text-align: center; padding: 32px 28px; background: rgba(255,255,255,0.06); border-radius: 22px; border: 1px solid rgba(255,255,255,0.12); color: #fff; }
.boxing-intro h3, .boxing-end h3 { font-family: 'Poppins', sans-serif; font-size: 1.6rem; margin-bottom: 12px; }
.boxing-intro p, .boxing-end p { color: rgba(255,255,255,0.85); line-height: 1.6; margin-bottom: 20px; }
.boxing-intro .accent { color: var(--amber); font-weight: 800; }

.boxing-arena { flex: 1; display: none; flex-direction: column; min-height: 0; position: relative; overflow: hidden; }
.boxing-arena.is-active { display: flex; }
.boxing-ring { flex: 1; position: relative; background: radial-gradient(ellipse 120% 80% at 50% 30%, #1a2838, #0a0e14 70%); }
.boxing-ring-floor { position: absolute; bottom: 0; left: -20%; right: -20%; height: 38%; background: linear-gradient(180deg, #2c3e50, #1a2330); transform: rotateX(58deg); transform-origin: bottom; border-top: 4px solid rgba(240,147,43,0.6); }
.boxing-ropes { position: absolute; inset: 12% 8% 28%; border: 3px solid rgba(255,255,255,0.08); border-radius: 8px; pointer-events: none; }
.boxing-opponent { position: absolute; left: 50%; top: 16%; transform: translateX(-50%); width: min(260px, 40vw); display: flex; flex-direction: column; align-items: center; }
.opp-head { width: 84px; height: 92px; border-radius: 50% 50% 46% 46%; background: linear-gradient(180deg, #d4a574, #a67c52); border: 3px solid rgba(0,0,0,0.2); box-shadow: 0 8px 24px rgba(0,0,0,0.35); position: relative; z-index: 3; }
.opp-head::after { content: ''; position: absolute; inset: 20% 22% 48% 22%; background: #2c3e50; border-radius: 50%; }
.opp-body { width: 130px; height: 150px; margin-top: -10px; background: linear-gradient(180deg, #c0392b, #922b21); border-radius: 22px; z-index: 2; box-shadow: 0 12px 32px rgba(0,0,0,0.4); }
.opp-gloves-row { display: flex; gap: 90px; margin-top: -24px; z-index: 4; }
.opp-glove { width: 52px; height: 52px; border-radius: 50%; background: linear-gradient(145deg, #e74c3c, #c0392b); border: 3px solid #922b21; }
.boxing-opponent.hit { animation: opp-hit 0.45s ease; }
.boxing-opponent.dodge { animation: opp-dodge 0.5s ease; }
@keyframes opp-hit { 25% { transform: translateX(-50%) rotate(-5deg) scale(0.96); filter: brightness(1.25); } 50% { transform: translateX(-48%) translateY(10px); } }
@keyframes opp-dodge { 40% { transform: translateX(-42%); } }
.boxing-screen-shake { animation: screen-shake 0.4s ease; }
@keyframes screen-shake { 20% { transform: translate(-8px,4px); } 40% { transform: translate(8px,-4px); } 60% { transform: translate(-5px,-2px); } 80% { transform: translate(5px,2px); } }

.boxing-hud-top { position: absolute; top: 0; left: 0; right: 0; z-index: 20; padding: 56px 16px 8px; display: flex; justify-content: space-between; gap: 12px; pointer-events: none; }
.boxing-health-bar { flex: 1; max-width: 180px; }
.boxing-health-bar label { font-family: 'Poppins', sans-serif; font-size: 0.68rem; font-weight: 700; color: rgba(255,255,255,0.7); text-transform: uppercase; }
.boxing-health-track { height: 11px; background: rgba(0,0,0,0.45); border-radius: 999px; overflow: hidden; margin-top: 4px; border: 1px solid rgba(255,255,255,0.12); }
.boxing-health-fill { height: 100%; transition: width 0.4s ease; }
.boxing-health-fill.you { background: linear-gradient(90deg, #6ab04c, #4e8a34); width: 100%; }
.boxing-health-fill.opp { background: linear-gradient(90deg, #e74c3c, #c0392b); width: 100%; }
.boxing-round-badge { font-family: 'Poppins', sans-serif; font-weight: 800; font-size: 0.85rem; background: rgba(0,0,0,0.5); color: var(--amber); padding: 8px 14px; border-radius: 999px; align-self: flex-end; }

.boxing-question-panel { position: absolute; top: 48%; left: 50%; transform: translate(-50%, -50%); z-index: 15; width: min(94%, 700px); text-align: center; }
.boxing-question-text { font-family: 'Poppins', sans-serif; font-weight: 800; font-size: clamp(1.6rem, 5vw, 2.6rem); color: #fff; text-shadow: 0 4px 20px rgba(0,0,0,0.8); margin-bottom: 18px; }
.boxing-options-row { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
.boxing-option { background: rgba(255,255,255,0.1); border: 3px solid rgba(255,255,255,0.22); border-radius: 16px; padding: 18px 12px; cursor: pointer; font-family: 'Poppins', sans-serif; font-weight: 800; font-size: clamp(1.3rem, 4vw, 1.9rem); color: #fff; transition: transform 0.2s, border-color 0.2s; }
.boxing-option.left { border-color: rgba(106,176,76,0.45); }
.boxing-option.right { border-color: rgba(52,152,219,0.45); }
.boxing-option.hot { transform: scale(1.04); border-color: var(--amber); background: rgba(240,147,43,0.2); }
.boxing-option.locked { border-color: var(--amber); box-shadow: 0 0 20px rgba(240,147,43,0.45); }
.boxing-option.correct { border-color: #6ab04c; background: rgba(106,176,76,0.35); }
.boxing-option.wrong { border-color: #e74c3c; background: rgba(231,76,60,0.3); }
.boxing-option.missing { border-color: var(--amber); background: rgba(245,215,110,0.35); color: var(--navy); }
.boxing-option small { display: block; font-size: 0.62rem; letter-spacing: 0.08em; margin-bottom: 4px; opacity: 0.9; }
.boxing-option:disabled { cursor: default; opacity: 0.95; }

.boxing-punch-hint { position: absolute; bottom: 20%; left: 50%; transform: translateX(-50%); z-index: 12; font-family: 'Poppins', sans-serif; font-weight: 700; font-size: 0.88rem; color: rgba(255,255,255,0.8); background: rgba(0,0,0,0.45); padding: 8px 18px; border-radius: 999px; }
.boxing-player-gloves { position: absolute; bottom: 6%; left: 0; right: 0; display: flex; justify-content: space-between; padding: 0 6%; pointer-events: none; z-index: 10; }
.boxing-p-glove { width: 72px; height: 72px; border-radius: 50%; background: linear-gradient(145deg, #3498db, #2980b9); border: 4px solid #1a5276; box-shadow: 0 8px 24px rgba(0,0,0,0.45); }
.boxing-p-glove.punch-l { animation: p-punch-l 0.35s ease; }
.boxing-p-glove.punch-r { animation: p-punch-r 0.35s ease; }
@keyframes p-punch-l { 50% { transform: translate(50px,-35px) scale(1.12); } }
@keyframes p-punch-r { 50% { transform: translate(-50px,-35px) scale(1.12); } }

.boxing-cam-pip { position: absolute; bottom: 12px; right: 12px; z-index: 25; width: 130px; height: 98px; border-radius: 10px; overflow: hidden; border: 2px solid rgba(255,255,255,0.25); background: #111; }
.boxing-cam-pip video { width: 100%; height: 100%; object-fit: cover; transform: scaleX(-1); }
.boxing-cal { position: absolute; bottom: 4px; left: 50%; transform: translateX(-50%); font-size: 0.68rem; font-weight: 700; color: var(--amber); background: rgba(0,0,0,0.75); padding: 3px 8px; border-radius: 999px; opacity: 0; white-space: nowrap; }
.boxing-cal.show { opacity: 1; }
.boxing-flash-msg { position: absolute; top: 40%; left: 50%; transform: translate(-50%,-50%); z-index: 22; font-family: 'Poppins', sans-serif; font-weight: 800; font-size: clamp(1.1rem, 4vw, 1.6rem); color: #fff; padding: 10px 24px; border-radius: 999px; background: rgba(0,0,0,0.8); opacity: 0; pointer-events: none; transition: opacity 0.2s; }
.boxing-flash-msg.show { opacity: 1; }
